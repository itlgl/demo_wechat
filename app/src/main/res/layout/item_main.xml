<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent" android:layout_height="@dimen/item_main_height"
    android:background="@drawable/selector_item_main_background">
    <ImageView
        android:id="@+id/image"
        android:layout_width="@dimen/item_main_height"
        android:layout_height="@dimen/item_main_height"
        android:layout_marginLeft="10dp"
        android:layout_marginRight="10dp"
        android:padding="8dp"/>

    <TextView
        android:id="@+id/text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:textColor="#000"
        android:textSize="@dimen/item_main_text_size"
        android:layout_toRightOf="@+id/image"/>

</RelativeLayout>